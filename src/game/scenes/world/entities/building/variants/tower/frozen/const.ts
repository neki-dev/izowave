export const BUILDING_TOWER_FROZEN_COST = 40; // Building cost

export const BUILDING_TOWER_FROZEN_HEALTH = 400; // Health

export const BUILDING_TOWER_FROZEN_ALLOW_BY_WAVE = 4; // Minimal wave for allow build

export const BUILDING_TOWER_FROZEN_RADIUS = 130; // Freeze radius

export const BUILDING_TOWER_FROZEN_RADIUS_GROWTH = 0.15; // Radius growth by level (Linear)

export const BUILDING_TOWER_FROZEN_DELAY = 1400; // Pause between freezes

export const BUILDING_TOWER_FROZEN_DELAY_GROWTH = -0.11; // Pause growth by level (Linear)

export const BUILDING_TOWER_FROZEN_FREEZE_DURATION = 800; // Freeze duration

export const BUILDING_TOWER_FROZEN_SHOT_SPEED = 300; // Shot speed
